% !TEX program = lualatex
% !TEX TS-program = lualatex
% main.tex -- your document. Build and check it with
%   make -f Makefile.a11y check
% Every kind of content, tagged and explained, is in example.tex. Find the block
% you need by its banner line (search example.tex for "%----") and copy it here.

%------------------------------------------------------------------------------
% METADATA AND CLASS
%------------------------------------------------------------------------------
% Keep this block above \documentclass: it turns tagging on and names the
% standards. Meets: ISO 14289-2:2024 8.11 (metadata); ISO 19005-4:2020.
\DocumentMetadata{
  lang        = en-US,
  pdfstandard = ua-2,
  pdfstandard = a-4f,
  tagging     = on,
  check-tagging-status,
}
% [report] for chapters ([book] too, but delete the abstract: book has none);
% align=justified for justified text; the other options are in README.md. A
% misspelt key=value option stops the build.
\documentclass{clemsona11y}

%------------------------------------------------------------------------------
% PACKAGES
%------------------------------------------------------------------------------
% The class already loads amsmath, amsthm, graphicx, hyperref, enotez, fontspec
% and unicode-math. Add only what you use; README.md ("Packages by field")
% says which packages tag correctly and which to avoid.
\usepackage{booktabs}  % \toprule, \midrule and \bottomrule in tables

%------------------------------------------------------------------------------
% PICTURES
%------------------------------------------------------------------------------
% Put every figure and image in the resources/ folder next to this file;
% \includegraphics[alt={...}]{name} finds it there by file name. Only pictures
% are looked up there: references.bib and any \input file stay beside main.tex.
% The example's pictures live there too; example.tex still needs them.

%------------------------------------------------------------------------------
% TITLE AND ABSTRACT
%------------------------------------------------------------------------------
% pdftitle: doubled braces keep commas in one title. pdfauthor: commas between names.
\title[pdftitle={{Title of the document}}]{Title of the document}
\author[pdfauthor={First Author, Second Author}]{First Author and Second Author}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
One paragraph that says what the document is about.
\end{abstract}

%------------------------------------------------------------------------------
% CONTENTS AND LISTS
%------------------------------------------------------------------------------
% Uncomment the ones the document needs.
% \tableofcontents
% \listoffigures
% \listoftables

%------------------------------------------------------------------------------
% BODY
%------------------------------------------------------------------------------
\section{Introduction}
Write here. Use \verb|\section|, \verb|\subsection| and \verb|\subsubsection| in order
and never skip a level. Give every picture alt text, declare the header cells of every
table, and write \verb|\mathalt{spoken text}| before each formula; \texttt{example.tex}
shows each of these once.

%------------------------------------------------------------------------------
% ENDNOTES AND BIBLIOGRAPHY
%------------------------------------------------------------------------------
% \printendnotes prints the endnotes written with \endnote. Uncomment the four
% bibliography lines once something is cited (entries go in references.bib); with
% no \cite they print an empty References heading. make runs BibTeX; so does
% VS Code once "% !BIB program = bibtex" and "% !BIB TS-program = bibtex" sit
% under line 2; TeXShop needs Typeset, BibTeX, Typeset, Typeset by hand.
\printendnotes
% \phantomsection
% \addcontentsline{toc}{section}{\refname}   % {chapter}{\bibname} with [report]
% \bibliographystyle{plain}
% \bibliography{references}
\end{document}
